import IconAnimatorPage from "./icon-animator";

export const metadata = {
    title: "Icon Animator v3",
    description:
        "Paste SVG, animate visually, export as motion/react component — v3 with auto-play loop, select all, auto-parse on paste, download .tsx, and a fully English UI.",
};

export default function Page() {
    return <IconAnimatorPage />;
}
