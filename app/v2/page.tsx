import IconAnimatorPage from "./icon-animator";

export const metadata = {
    title: "Icon Animator v2",
    description:
        "Paste SVG, animate visually, export as motion/react component — v2 with ring/shake, 3D transforms, color animation.",
};

export default function Page() {
    return <IconAnimatorPage />;
}
